(library
 (name cnlsp)
 (libraries base cn linol linol-lwt lsp)
 (preprocess
  (pps ppx_deriving.enum ppx_enumerate)))

(env
 (dev
  (flags
   ;; Disable promotion of warnings to errors
   (:standard -warn-error -A))))
